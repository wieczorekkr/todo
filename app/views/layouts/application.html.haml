!!!
%html
	%head
		%title Todo
		= stylesheet_link_tag    "application", media: "all"
		= javascript_include_tag "application"
		= csrf_meta_tags
	%body
		.row-fluid
			.span10.offset1
				.hero-unit.text-center
					%h1
						ToDo
					%p
						Witajcie w tutorialu aplikacji ToDo
					.text-center
					= link_to 'Nowe zadanie', new_task_path, class: 'btn btn-primary', remote: true
					.text-center
						%strong= current_user.email
					.text-center
						= link_to 'wyloguj', destroy_user_session_path, method: :delete
		= yield
